<div id="map">
    <!-- Create map here -->
</div>

<% content_for :sidebar do %>
    <h3>Event Details</h3>
    <p>Insert some details about <a href="/promoter/<%= @promoter.id %>" title="<%= @promoter.name %>"><%= @promoter.name %></a> and the <%= @event.title %> in here</p>
    <p>Description:</p>
    <p><%= @event.description %> </p>
<% end %>

<% content_for :js_footer do %>
    <script src="../../js/init.js"></script>
    <script>
        $(document).ready(function() {
            var map = createMap(<%= @event.location[1] %>, <%= @event.location[0] %>, 16, "map");
            var marker = dropPin(<%= @event.location[1] %>, <%= @event.location[0] %>, map, false); 
        });
    </script>
<% end %>